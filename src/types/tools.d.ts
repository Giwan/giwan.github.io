export type TTool = {
    title: string;
    url: string;
    description: string;
    price: number;
    currency?: string;
    category: string;
    labels: string[];
    dateAdded?: string;
    lastVerified?: string;
}